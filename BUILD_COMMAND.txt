!!! RENDER — שנה ב-Dashboard !!!

האפשרות המומלצת:
  Environment = Docker
  Dockerfile Path = Dockerfile

אם נשאר Node, Build Command חייב להיות אחד מאלה:
  bash render-build.sh
  npm ci && npm run build

אסור:
  pm install && npm run build

פרטים: docs/DEPLOY-NOW-HE.md
